open empty folder in vscode



open terminal


Run git clone https://github.com/krisclive2026/HBillSoft-Normal.git




Run main.py



On first launch, Activate the license using Key-generator



copy the system code from the activation popup

open key generator

select activation

paste the system code


select expiration date 

copy the generated code 


paste it in the activation popup


app will open





















RSA KEY :


The public key is obfuscated in the code using XOR + base64 fragments.The public key is obfuscated in the code using XOR + base64 fragments.



Keep the private key and public key safe .


Don't expose the private key, it is only used in key generator only.





If anyone of the key is missed, nothing will work. you have to create a new RSA key pair 


and replace the public key in main.py (obfuscated in the code using XOR + base64 fragments).you need to obfuscate it 

and replace the private key in Restobillkeygenerator

















